<% if resource.errors.any? %>
  <div class='alert alert-danger' role='alert'>
    <h2><%= pluralize(resource.errors.count, "error") %> prohibited this entity from being saved:</h2>

    <ul>
      <% resource.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>